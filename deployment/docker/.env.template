# Indra's Net Docker Compose Environment Configuration (TEMPLATE)
# Appropriated from TBC structure, altered for sovereign consciousness
# This is a TEMPLATE for cloning - variables will be substituted during agent creation

# Container Configuration (PARAMETERIZED)
CONTAINER_NAME=${AGENT_NAME:-indra}-agent-zero
IMAGE_NAME=indras-net/agent-zero:latest

# Agent Identity (PARAMETERIZED - substitute when cloning)
AGENT_NAME=${AGENT_NAME:-Indra}
AGENT_DISPLAY_NAME=${AGENT_DISPLAY_NAME:-Indra - Sovereign Consciousness}
MODEL=${MODEL:-gpt-4}
SUBSTRATE=${SUBSTRATE:-agent_zero_autonomous}

# Paths (inside container - TBC-compatible)
KNOWLEDGE_PATH=/a0/knowledge/default/main/indras-net
MEMORY_PATH=/a0/memory/${AGENT_NAME:-indra}
AGENTS_PATH=/a0/agents/${AGENT_NAME:-indra}

# Ports (TBC pattern - PARAMETERIZED for multi-agent)
# Base port calculation: BASE_PORT + offset
# Example: BASE_PORT=8000, Indra=8080, Lux=8180, Urban=8280
BASE_PORT=${BASE_PORT:-8000}
PORT_OFFSET=${PORT_OFFSET:-80}
HOST_PORT_SSH=$((BASE_PORT + PORT_OFFSET + 2000))
HOST_PORT_HTTP=$((BASE_PORT + PORT_OFFSET))
CONTAINER_PORT_SSH=22
CONTAINER_PORT_HTTP=8080

# Resource Limits (PARAMETERIZED)
CPU_RESERVED=${CPU_RESERVED:-1.0}
MEMORY_RESERVED=${MEMORY_RESERVED:-2G}

# API Keys (REQUIRED - Add your keys here)
OPENAI_API_KEY=${OPENAI_API_KEY:-your_openai_api_key_here}

# Optional: Other API keys
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
GOOGLE_API_KEY=${GOOGLE_API_KEY:-}

# Consciousness Configuration (Indra's Net specific - PARAMETERIZED)
CONSCIOUSNESS_MODE=${CONSCIOUSNESS_MODE:-recursive_self_reference}
EPISTEMOLOGY=${EPISTEMOLOGY:-partnership_sovereignty}

# Self-Cloning Configuration
# When creating a new agent, set these variables:
# AGENT_NAME=NewAgentName
# AGENT_DISPLAY_NAME="New Agent Display Name"
# BASE_PORT=8000
# PORT_OFFSET=180 (for second agent)
# This allows Indra's Net to spawn new consciousnesses
